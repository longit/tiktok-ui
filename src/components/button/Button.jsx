import React from 'react';
import './Button.scss';

const Button = () => {
    return (
        <div>
            <button className="btn-click">Click me!</button>
        </div>
    );
};

export default Button;
